package com.student.clearance.system.repository.semesterControl;

import com.student.clearance.system.domain.semesterControl.SemesterControl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SemesterControlRepository extends JpaRepository<SemesterControl, Long> {
}
